name: staging

on:
  workflow_dispatch:
  push:
    branches:
    - main

permissions:
  contents: read
  packages: write

jobs:
  lint:
    uses: potber/.github/.github/workflows/npm_lint.yml@v1

  test:
    uses: potber/.github/.github/workflows/npm_test-playwright.yml@v1

  release_image:
    needs:
    - lint
    - test
    permissions:
      contents: read
      packages: write
    uses: potber/.github/.github/workflows/release_image.yml@v1
    with:
      latest: true
